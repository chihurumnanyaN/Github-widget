## Setup

1. Clone the repo
2. Run `npm install`
3. Copy `.env.example` to `.env` and fill in your values
4. Get a GitHub token at https://github.com/settings/tokens (needs `read:user` scope)
5. Run `npm start`


GitHub Contribution Widget
A lightweight desktop widget that displays your GitHub contribution activity (the green dots) directly on your screen in real time.
Built with Electron, this app allows developers to visualize their GitHub activity without opening the browser — perfect for staying motivated and tracking consistency at a glance.

✨ Features
📊 Displays your GitHub contribution graph
🔄 Fetches live data using the GitHub API
🖥 Runs as a lightweight desktop widget
🔐 Secure authentication using a GitHub personal access token
⚡ Simple setup and minimal configuration

🛠 Tech Stack

Electron
JavaScript
GitHub API
dotenv for environment variable management

🚀 Use Case

Ideal for developers who want:
A productivity reminder on their desktop
A visual contribution tracker
A clean GitHub stats display without opening GitHub
